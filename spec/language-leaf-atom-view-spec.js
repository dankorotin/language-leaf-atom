'use babel';

import LanguageLeafAtomView from '../lib/language-leaf-atom-view';

describe('LanguageLeafAtomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
